# Report: J-20260216-005 - Migrate mute/timer/TTS controls into DialogueLearningSettingDialog

## Summary (What changed)
- `DialogueLearningSettingDialog`에 음소거/학습 타이머/TTS 섹션을 실제 컨트롤 형태로 반영했다.
- 다이얼로그에서 `AppSettingsStore`를 직접 바인딩해 음소거, TTS 속도, TTS 로케일 저장/복원을 구현했다.
- `SettingFragment`와 `fragment_setting.xml`에서 이동 대상 3개 섹션(mute/timer/TTS)과 관련 바인딩/리스너를 제거해 LLM 설정만 남겼다.
- 학습 화면 런타임 로그 태그를 `JOB_J-20260216-005`로 맞춰 QA 필터 기준을 통일했다.
- 다이얼로그 안내 문구를 현재 동작 상태에 맞게 조정했다.

## Files touched (Actual)
- `repo/app/src/main/java/com/example/test/dialog/DialogueLearningSettingDialog.java`
- `repo/app/src/main/java/com/example/test/fragment/DialogueLearningFragment.java`
- `repo/app/src/main/java/com/example/test/fragment/SettingFragment.java`
- `repo/app/src/main/res/layout/dialog_dialogue_learning_setting.xml`
- `repo/app/src/main/res/layout/fragment_setting.xml`
- `repo/app/src/main/res/values/strings.xml`

## How to test (Step-by-step)
1) 설정 탭(`SettingFragment`) 진입 후 LLM 설정 섹션만 남아 있고, 음소거/학습 타이머/TTS 섹션이 제거되었는지 확인한다.
2) 학습 화면 진입 후 우측 상단 설정 버튼(`btn_right`)으로 `DialogueLearningSettingDialog`를 연다.
3) 다이얼로그에서 `모든 재생 음소거`를 ON/OFF하고, 메시지 TTS/스크립트 TTS 재생 시 차단/허용 동작이 반영되는지 확인한다.
4) TTS 속도와 로케일(프리셋/직접입력)을 변경한 뒤 재생해서 적용되는지 확인한다.
5) 다이얼로그를 닫았다가 다시 열어 저장 값이 유지되는지 확인한다.

## Build Result
- Ran:
  - `./gradlew :app:spotlessApply :app:spotlessCheck :app:assembleDebug :app:testDebugUnitTest --console=plain`
- Result: PASS
- Notes:
  - `BUILD SUCCESSFUL in 16s`
  - `:app:testDebugUnitTest`는 `NO-SOURCE`로 종료됨(프로젝트 현 상태 기준)

## Logcat
- Tag: `JOB_J-20260216-005`
- Filter:
  - `adb logcat | grep JOB_J-20260216-005`
- Expected:
  - 다이얼로그에서 mute 저장 시 `Saved mute setting: ...`
  - TTS 속도 저장 시 `Saved TTS speech rate.`
  - 로케일 저장 시 `Saved TTS locale.`
  - 재생 차단/적용 시 `DialogueLearningFragment`의 runtime 로그 출력
- Observed:
  - 본 단계에서는 단말/에뮬레이터 수동 QA를 수행하지 않아 Logcat 실측은 미수집

## QA Checklist (Copy from Job + Observations)
### UX
- [ ] 시나리오 1: 학습 화면 세팅 다이얼로그에서 mute ON 시 재생이 차단되고 OFF 시 정상 재생된다.
- [ ] 시나리오 2: TTS 속도/로케일 변경 후 학습 화면 재생에 변경값이 적용된다.
- [ ] 시나리오 3: 타이머 섹션은 기존과 동일하게 placeholder/비활성 상태 의미를 유지한다.
- [ ] 시나리오 4: 기존 설정 탭(`SettingFragment`)에서 이동된 3개 섹션이 제거되어 중복 노출되지 않는다.

### Logcat
- [ ] 기대 로그 1: 설정 저장 이벤트가 DEBUG 가드 하에 출력된다.
- [ ] 기대 로그 2: 민감정보 없이 적용/차단 이벤트 로그가 출력된다.

## Risks / Edge cases
- 타이머 섹션은 의도적으로 placeholder 상태이며 실제 학습 제어 로직과 연결하지 않았다.
- 작업 트리에 `repo/AGENTS.md` 삭제 변경이 존재하며, 사용자 지시(삭제 상태 유지)에 따라 그대로 유지했다.

## Rollback
- `J-20260216-005` 관련 변경 파일만 부분 revert하면 `SettingFragment` 기반 기존 설정 구조로 복구할 수 있다.
