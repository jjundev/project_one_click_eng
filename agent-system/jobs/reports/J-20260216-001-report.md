# Report: J-20260216-001 - MainActivity BottomNavigation Fragment 전환 추가

## Summary (What changed)
- `MainActivity`에 `BottomNavigationView`와 `NavController`를 연결해 하단 탭 기반 화면 전환을 구현했습니다.
- 탭 선택 시 목적지로 이동하도록 처리하고, 목적지 변경 시 하단 탭 선택 상태를 동기화했습니다.
- `JOB_J-20260216-001` 태그로 탭 선택/Fragment 전환 로그를 `BuildConfig.DEBUG` 가드 하에 추가했습니다.
- `activity_main.xml`을 세로 레이아웃으로 재구성해 상단 Fragment 영역 + 하단 BottomNavigation 구조로 변경했습니다.
- 하단 탭 3개(Mode/Scripts/Summary)용 메뉴 리소스 및 문자열을 추가했습니다.

## Files touched (Actual)
- `repo/app/src/main/java/com/example/test/activity/MainActivity.java`
- `repo/app/src/main/res/layout/activity_main.xml`
- `repo/app/src/main/res/menu/menu_main_bottom_nav.xml`
- `repo/app/src/main/res/values/strings.xml`

## How to test (Step-by-step)
1) `repo` 디렉터리에서 `./gradlew :app:assembleDebug testDebugUnitTest` 실행
2) 앱 실행 후 MainActivity 하단에 BottomNavigation(Mode/Scripts/Summary) 표시 확인
3) 각 탭을 탭할 때 대응 Fragment로 즉시 전환되고 크래시가 없는지 확인
4) 화면 회전/백그라운드 복귀 후 현재 선택 탭과 표시 화면이 일치하는지 확인
5) `adb logcat | grep JOB_J-20260216-001`로 탭 선택/전환 로그 확인

## Build Result
- Ran:
  - `./gradlew :app:assembleDebug testDebugUnitTest`
- Result:
  - PASS
- Notes:
  - `testDebugUnitTest`는 NO-SOURCE로 완료되었습니다.

## Logcat
- Tag: `JOB_J-20260216-001`
- Filter:
  - `adb logcat | grep JOB_J-20260216-001`
- Expected:
  - 탭 선택 시 `Tab selected: <destination_name> (<id>)`
  - Fragment 전환 시 `Fragment switched: <destination_name> (<id>)`
- Observed:
  - 로컬 빌드 환경에서는 디바이스 실행/Logcat 수집을 수행하지 못했습니다.

## QA Checklist (Copy from Job + Observations)
### UX
- [ ] 시나리오 1: 앱 실행 후 BottomNavigation의 기본 선택 탭과 해당 Fragment가 정상 표시된다. (미검증: 사용자 디바이스 확인 필요)
- [ ] 시나리오 2: 모든 탭을 1회 이상 왕복 전환해도 화면 깨짐/중복 표시/크래시가 없다. (미검증: 사용자 디바이스 확인 필요)
- [ ] 시나리오 3: 백그라운드 전환 후 복귀 시 현재 탭과 화면이 일치한다. (미검증: 사용자 디바이스 확인 필요)

### Logcat
- [ ] 기대 로그 1: 탭 선택 시 선택 탭 id 또는 name 로그 출력 (코드 반영 완료, 디바이스 미검증)
- [ ] 기대 로그 2: Fragment 전환 완료 로그 출력 (코드 반영 완료, 디바이스 미검증)

## Risks / Edge cases
- BottomNavigation 메뉴 항목이 현재 `nav_graph`의 3개 destination id에 직접 매핑되어 있어, 향후 destination id 변경 시 메뉴 id도 함께 수정해야 합니다.
- 기본 Android drawable 아이콘을 사용하므로 디자인 가이드가 확정되면 커스텀 아이콘 교체가 필요할 수 있습니다.

## Rollback
- revert / feature flag / delete 변경 위치: `MainActivity`의 BottomNavigation wiring 제거 + `activity_main.xml`을 기존 단일 `FragmentContainerView` 레이아웃으로 복원 + `menu_main_bottom_nav.xml` 및 관련 문자열 삭제
