# Report: J-20260216-002 - Main Navigation Empty Fragment Wiring

## Summary (What changed)
- Bottom navigation was restructured from 3 tabs to 5 tabs in the requested order:
  - 영어학습 쇼츠, 게임 모드, 학습 모드, 학습한 내용 기록, 설정
- `studyModeSelectFragment` remains the nav graph start destination, preserving 학습 모드 default entry.
- Added 4 Empty Fragment destinations and 4 matching Fragment classes/layouts for unimplemented tabs.
- Updated `MainActivity` logging tag to `JOB_J-20260216-002` and guarded bottom-nav selection sync so non-tab destinations do not force invalid menu selection.

## Files touched (Actual)
- `repo/app/src/main/java/com/example/test/activity/MainActivity.java`
- `repo/app/src/main/res/menu/menu_main_bottom_nav.xml`
- `repo/app/src/main/res/navigation/nav_graph.xml`
- `repo/app/src/main/res/values/strings.xml`
- `repo/app/src/main/java/com/example/test/fragment/EnglishShortsEmptyFragment.java`
- `repo/app/src/main/java/com/example/test/fragment/GameModeEmptyFragment.java`
- `repo/app/src/main/java/com/example/test/fragment/LearningHistoryEmptyFragment.java`
- `repo/app/src/main/java/com/example/test/fragment/SettingsEmptyFragment.java`
- `repo/app/src/main/res/layout/fragment_english_shorts_empty.xml`
- `repo/app/src/main/res/layout/fragment_game_mode_empty.xml`
- `repo/app/src/main/res/layout/fragment_learning_history_empty.xml`
- `repo/app/src/main/res/layout/fragment_settings_empty.xml`

## How to test (Step-by-step)
1) Launch app on debug build.
2) Verify default selected tab is `학습 모드` and first screen is `LearningModeSelectFragment`.
3) Tap each tab in order: 영어학습 쇼츠, 게임 모드, 학습한 내용 기록, 설정.
4) Verify each tab shows the target empty screen with `준비 중입니다.` and no crash.
5) Rapidly switch across all 5 tabs multiple times and verify no freeze/crash.
6) Capture logs with `adb logcat | grep JOB_J-20260216-002` while switching tabs.

## Build Result
- Ran:
  - `.\gradlew.bat :app:spotlessApply :app:spotlessCheck :app:assembleDebug :app:testDebugUnitTest`
- Result: PASS
- Notes:
  - `assembleDebug` successful.
  - `testDebugUnitTest` reported `NO-SOURCE` (no unit tests defined for this variant), build still passed.

## Logcat
- Tag: `JOB_J-20260216-002`
- Filter:
  - `adb logcat | grep JOB_J-20260216-002`
- Expected:
  - Tab selection logs include selected menu resource name and id.
  - Destination change logs include destination resource name and id for each Empty Fragment.
- Observed:
  - CLI environment did not run an Android device/emulator session, so runtime Logcat capture is pending QA execution.

## QA Checklist (Copy from Job + Observations)
### UX
- [ ] 시나리오 1: 앱 실행 직후 `학습 모드` 탭이 기본 선택되고 `LearningModeSelectFragment`가 보인다.
- [ ] 시나리오 2: `영어학습 쇼츠`, `게임 모드`, `학습한 내용 기록`, `설정` 탭 각각 진입 시 Empty 상태 문구가 정상 노출된다.
- [ ] 시나리오 3: 5개 탭을 연속 전환해도 화면 멈춤/크래시가 없다.

### Logcat
- [ ] 기대 로그 1: 탭 선택 로그가 각 메뉴 ID로 출력된다.
- [ ] 기대 로그 2: 목적지 전환 로그가 각 Empty Fragment destination ID로 출력된다.

## Risks / Edge cases
- `scriptSelectFragment`/`dialogueSummaryFragment` are still reachable by internal flow but are no longer bottom-nav tabs; selected tab remains on the last top-level tab, which is intended.
- Final runtime verification for tab switching stability depends on QA device/emulator execution.

## Rollback
- Revert navigation/menu/activity and remove new placeholder fragment/layout files:
  - `repo/app/src/main/res/menu/menu_main_bottom_nav.xml`
  - `repo/app/src/main/res/navigation/nav_graph.xml`
  - `repo/app/src/main/java/com/example/test/activity/MainActivity.java`
  - `repo/app/src/main/res/values/strings.xml`
  - `repo/app/src/main/java/com/example/test/fragment/*EmptyFragment.java`
  - `repo/app/src/main/res/layout/fragment_*_empty.xml`
